import { StyleSheet } from "react-native"

export const stylesSingIn = StyleSheet.create({
    container:{
        flex:1,
        justifyContent: 'center',
        alignItems:'center',
    },
})